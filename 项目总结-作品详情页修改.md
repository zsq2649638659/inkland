# 作品详情页（Post Detail Page）修改总结

## 1. 用户核心需求

用户正在开发一个名为 **inkland** 的同人创作社区平台（fanfic-platform），技术栈为 **Next.js 14 (App Router) + TypeScript + Supabase + Tailwind CSS v4 + Font Awesome 6**。本次工作的核心目标是**修复和优化单篇作品详情页（即阅读器页面）**，包括：

- 阅读器基础功能（字体/字号/主题/宽度设置）
- 段评系统（段落级评论）的交互与样式
- 文章底部评论区（顶级评论 + 楼中楼回复）的完整实现
- 浮动侧边栏（Floating Sidebar）与浮动面板（Floating Panel）的定位联动
- 整体 UI 风格与设计稿对齐

---

## 2. 已完成的修改

### 2.1 浮动侧边栏与面板定位联动

- **状态追踪**：在 `ReaderClient.tsx` 中通过 `useRef(contentRef)` 监听 `content-wrapper` 的 `getBoundingClientRect().right`，计算 `panelRight` 值
- **动态定位**：`floating-sidebar` 使用 `right: ${panelRight + 48}px`，`floating-panel` 使用 `right: ${panelRight}px`，覆盖 CSS 中的静态值
- **事件监听**：绑定 `resize` 和 `scroll` 事件，在 `readerWidth` 变化时重新计算位置，延迟 50ms 执行

### 2.2 段评系统（Paragraph Comment Panel）

- **段落点击**：文字作品（novel/serial/article）支持点击段落触发段评面板
- **底部弹出面板**：使用 `para-comment-panel-overlay` + `para-comment-panel` 实现 bottom sheet 效果，带遮罩层和动画过渡
- **评论加载**：按 `paragraph_index` 筛选，加载统计数据（`comment_stats`）和点赞状态（`comment_likes`）
- **排序规则**：按点赞数排序，2个高赞评论置顶，其余按时间倒序
- **楼中楼回复**：支持展开/收起回复，动态加载回复数据，计算楼层号

### 2.3 文章底部评论区

- **评论输入**：支持已登录用户输入评论，包含 Emoji 选择器（`EmojiPicker`）
- **评论列表**：展示顶级评论，支持点赞、回复、举报、屏蔽用户
- **回复楼中楼**：在 `ReaderClient.tsx` 中，`replies` 状态管理每个顶级评论的回复列表，以缩进（`comment-replies`，`margin-left: 48px`）展示
- **空状态**：使用 `EmptyState` 组件的 `compact` 模式（`icon: "fa-comment-dots"`, `title: "暂无评论，成为第一个评论的人吧"`）

### 2.4 阅读器设置

- **字体**：支持思源黑体（sans）、思源宋体（serif）、霞鹜文楷（kai）
- **字号**：12px-40px 滑动条调节
- **主题**：white/warm/beige/green/blue/dark 六种配色方案
- **页面宽度**：auto/640/800/900/1000/1280 六档
- **暗色模式**：通过 `data-theme="dark"` 属性切换，CSS 变量驱动

### 2.5 其他功能

- **上一章/下一章**：连载作品支持章节导航
- **阅读进度保存**：长篇连载自动保存阅读进度到 localStorage
- **举报系统**：弹窗式举报表单，6种举报原因
- **屏蔽用户**：屏蔽后自动过滤该用户的评论

---

## 3. 当前待处理的问题（最新反馈的6个问题）

### 问题 1：Floating Sidebar 位置跟随页面宽度变化，段评宽度同步变化

**现状分析**：
- `floating-sidebar` 的 CSS 硬编码 `right: 48px`，但组件内通过 `style={{ right: `${panelRight + 48}px` }}` 覆盖
- `floating-panel` 的 CSS 硬编码 `right: 148px`，组件内通过 `style={{ right: `${panelRight}px` }}` 覆盖
- `contentRef` 追踪 `.content-wrapper` 的右边缘，`panelRight` 通过 `viewportWidth - rect.right` 计算
- 段评面板（`para-comment-panel`）当前 `max-width: 800px`，未与 readerWidth 联动

**需要修改的内容**：
- ReaderClient.tsx 第 417 行：`floating-sidebar` 的 `right` 定位
- ReaderClient.tsx 第 838 行：`floating-panel` 的 `right` 定位
- 段评面板的宽度应与内容宽度联动，而非固定 800px
- 确保 `updatePosition` 在 `panelRight` 变化时正确触发

### 问题 2：文章详情页底部评论空状态样式优化

**现状分析**：
- 当前 `ReaderClient.tsx` 第 628-629 行使用 `<EmptyState icon="fa-comment-dots" title="暂无评论，成为第一个评论的人吧" compact />`
- `EmptyState` 的 `compact` 模式渲染一个圆形图标 + 文字，样式较为简单
- 需要更丰富的空状态视觉设计（参考设计稿中的空状态样式）

**需要修改的内容**：
- ReaderClient.tsx 第 628-629 行：空状态组件的使用方式
- 可能需要扩展 EmptyState 组件或添加新的空状态样式
- 涉及 CSS：`.feed-empty-state` (第 824 行) 和 `.empty-state` (第 922 行) 已有相关样式

### 问题 3：楼中楼样式统一及交互优化

**现状分析**：
- 当前存在**两套楼中楼实现**：
  1. **ReaderClient.tsx**（第 704-778 行）：使用 `comment-replies` + `comment-reply` 类，采用缩进（`margin-left: 48px`）展示
  2. **ParagraphCommentPanel.tsx**（第 407-458 行）：使用 `nested-replies` + `nested-reply-item` 类，独立的楼中楼样式，支持展开/收起
- 两套样式在视觉上不统一，需要统一为同一套楼中楼样式

**需要修改的内容**：
- ReaderClient.tsx 第 704-778 行：评论回复区域，将 `comment-replies` / `comment-reply` 替换为统一的 `nested-replies` / `nested-reply-item` 样式
- 确保两处楼中楼的交互行为一致（hover 效果、展开/收起、更多按钮等）
- 同步更新 CSS 中相关样式，删除冗余的 `.comment-replies` / `.comment-reply` 样式

### 问题 4：段评评论布局：喜欢和回复位置调整

**现状分析**：
- `ParagraphCommentPanel.tsx` 第 341-367 行：`comment-actions` 中按钮顺序为「回复 → 喜欢」
- 当前设计稿要求「喜欢」按钮在前，「回复」按钮在后
- 同时在 hover 时显示的操作按钮（`.comment-more-btn`）对齐方式需要调整

**需要修改的内容**：
- ParagraphCommentPanel.tsx 第 341-355 行：调整 `comment-action-btn` 顺序，将「喜欢」按钮移到「回复」按钮之前
- 第 350-355 行：喜欢按钮的 `toggleLike` 调用逻辑不动，仅调整 DOM 顺序
- 同时调整 `comment-more-btn`（hover 显示）的显示位置

### 问题 5："谁回复谁"格式：A 回复 @B，可点击链接

**现状分析**：
- 当前回复内容以纯文本形式展示，没有"谁回复谁"的格式标识
- 用户期望格式如：`A 回复 @B`，其中 `@B` 部分可点击跳转到用户主页
- 格式仅适用于楼中楼回复（非顶级评论），需要识别被回复人的信息

**需要修改的内容**：
- 在楼中楼回复的 header 区域添加 `reply-to` 元素
- 格式：`<span class="reply-name">A</span> <span class="reply-to">回复 <a href="/user/B">@B</a></span>`
- 需要数据层面的支持：在加载回复时获取被回复人的用户信息
- 涉及文件：ParagraphCommentPanel.tsx（第 429-434 行）和 ReaderClient.tsx（第 717-722 行）
- CSS 中 `.reply-to` 类已有定义（第 3236-3248 行），但需要确认是否在楼中楼中使用

### 问题 6：评论输入框删除 `comment-input-name`，添加删除按钮

**现状分析**：
- `ReaderClient.tsx` 第 572-575 行：存在 `<h3 className="comment-input-name">` 元素，显示"评论 (X)"标题
- 该标题需要被删除，因为其样式与设计稿不符
- 同时需要在评论输入框区域添加一个"删除"按钮（清空输入内容）

**需要修改的内容**：
- ReaderClient.tsx 第 572-575 行：删除整个 `<h3 className="comment-input-name">` 区块
- 在评论输入框的 `comment-submit-row`（第 600-612 行）中添加一个删除/清空按钮
- 删除按钮逻辑：`setCommentText("")`，并在有文本内容时显示
- 添加删除按钮的 CSS 样式

---

## 4. 技术架构与代码模式

### 4.1 整体架构

```
fanfic-platform/
├── src/
│   ├── app/                    # Next.js App Router 页面
│   │   ├── layout.tsx          # 根布局（字体加载、主题初始化）
│   │   ├── page.tsx            # 首页（作品流）
│   │   ├── globals.css         # 全局样式 + 设计系统
│   │   ├── read/[id]/page.tsx  # 阅读器页面（Server Component）
│   │   └── ...                 # 其他页面
│   ├── components/             # React 组件
│   │   ├── ReaderClient.tsx    # 阅读器客户端组件（核心）
│   │   ├── ParagraphCommentPanel.tsx  # 段评面板
│   │   ├── EmptyState.tsx      # 空状态组件
│   │   ├── AuthProvider.tsx    # 认证上下文
│   │   ├── LikeButton.tsx      # 点赞按钮
│   │   ├── BookmarkButton.tsx  # 收藏按钮
│   │   ├── EmojiPicker.tsx     # Emoji 选择器
│   │   └── ...                 # 其他组件
│   └── lib/
│       ├── supabase/           # Supabase 客户端
│       └── types.ts            # TypeScript 类型定义
```

### 4.2 数据流模式

- **Server Component** (`read/[id]/page.tsx`): 直接使用 `fetch` 调用 Supabase REST API 获取作品数据、作者信息、标签
- **Client Component** (`ReaderClient.tsx`): 使用 `supabase` 客户端（`createClient()`）在浏览器端加载评论、统计、点赞状态
- **状态管理**：使用 React `useState` 和 `useEffect` 进行本地状态管理，无全局状态库
- **认证状态**：通过 `AuthProvider` 的 `useAuth()` hook 获取当前用户和 profile

### 4.3 样式模式

- **Tailwind CSS v4**: 使用 `@import "tailwindcss"` 导入，通过 `@theme inline` 定义设计 token
- **CSS 变量**: 设计系统通过 CSS 自定义属性（`--color-*`, `--radius-*`, `--spacing-*`）定义
- **暗色模式**: 通过 `[data-theme="dark"]` 选择器覆盖 CSS 变量值
- **BEM-like 类名**: 组件使用有意义的类名（如 `.floating-sidebar`, `.para-comment-panel`, `.comment-actions`）
- **响应式**: 通过 `@media (max-width: 768px)` 媒体查询处理移动端适配

### 4.4 关键交互模式

| 交互 | 实现方式 | 文件 |
|------|----------|------|
| 浮动侧边栏定位 | `useRef` + `getBoundingClientRect` | ReaderClient.tsx |
| 段评面板 | Bottom Sheet（overlay + panel + translateY） | ReaderClient.tsx + ParagraphCommentPanel.tsx |
| 评论楼中楼 | 嵌套展开/收起 + 动态数据加载 | ReaderClient.tsx + ParagraphCommentPanel.tsx |
| 点赞 | 乐观更新（先更新 UI，再发请求） | ParagraphCommentPanel.tsx |
| 主题切换 | CSS 变量 + `data-theme` 属性 | ReaderClient.tsx + globals.css |

---

## 5. 关键文件列表

| 文件路径 | 行数 | 角色 | 核心关注点 |
|----------|------|------|-----------|
| `/Users/diana/Documents/TRAE/同人网站/fanfic-platform/src/app/read/[id]/page.tsx` | 82 | Server Component | 数据获取层，负责从 Supabase 拉取作品数据 |
| `/Users/diana/Documents/TRAE/同人网站/fanfic-platform/src/components/ReaderClient.tsx` | 979 | 阅读器核心组件 | 浮动侧边栏、段落渲染、评论区、字型主题设置 |
| `/Users/diana/Documents/TRAE/同人网站/fanfic-platform/src/components/ParagraphCommentPanel.tsx` | 513 | 段评面板组件 | 段评评论列表、楼中楼展开、内联回复、点赞交互 |
| `/Users/diana/Documents/TRAE/同人网站/fanfic-platform/src/app/globals.css` | ~4320 | 全局样式 | 设计系统变量、所有组件样式、暗色模式、响应式 |
| `/Users/diana/Documents/TRAE/同人网站/fanfic-platform/src/components/EmptyState.tsx` | 76 | 空状态组件 | compact 和 full 两种模式 |
| `/Users/diana/Documents/TRAE/同人网站/fanfic-platform/src/lib/types.ts` | ~85 | 类型定义 | Post、Comment、Profile 等核心类型 |
| `/Users/diana/Documents/TRAE/同人网站/fanfic-platform/src/app/layout.tsx` | 49 | 根布局 | 字体加载、主题初始化脚本、Font Awesome 引入 |
| `/Users/diana/Documents/TRAE/同人网站/fanfic-platform/src/components/AuthProvider.tsx` | - | 认证上下文 | 提供 `user` 和 `profile` 给全局 |

### 关键代码段索引

**浮动侧边栏定位**（ReaderClient.tsx 第 90-107 行）：
```typescript
useEffect(() => {
  if (!contentRef.current) return;
  const updatePosition = () => {
    if (contentRef.current) {
      const rect = contentRef.current.getBoundingClientRect();
      const viewportWidth = window.innerWidth;
      setPanelRight(viewportWidth - rect.right);
    }
  };
  // ...
}, [readerWidth]);
```

**浮动侧边栏 DOM**（ReaderClient.tsx 第 417 行）：
```html
<div className="floating-sidebar" style={{ right: `${panelRight + 48}px` }}>
```

**段评面板**（ReaderClient.tsx 第 902-924 行）：
```html
<div className="para-comment-panel-overlay">
  <div className="para-comment-panel">
    <ParagraphCommentPanel ... />
  </div>
</div>
```

**评论空状态**（ReaderClient.tsx 第 628-629 行）：
```typescript
<EmptyState icon="fa-comment-dots" title="暂无评论，成为第一个评论的人吧" compact />
```

**楼中楼（ReaderClient.tsx 版本）**（第 704-778 行）：
```html
<div className="comment-replies">
  {replies[c.id].map((reply) => (
    <div className="comment-reply">...</div>
  ))}
</div>
```

**楼中楼（ParagraphCommentPanel.tsx 版本）**（第 407-458 行）：
```html
<div className="nested-replies">
  <button onClick={() => toggleExpandReplies(c.id)}>展开/收起</button>
  {expandedReplies.has(c.id) && c.replies && c.replies.length > 0 && (
    <div className="flex flex-col gap-3">
      {c.replies.map((reply) => (
        <div className="nested-reply-item">...</div>
      ))}
    </div>
  )}
</div>
```

**段评评论操作按钮**（ParagraphCommentPanel.tsx 第 341-355 行）：
```html
<div className="comment-actions">
  <button className="comment-action-btn" onClick={() => setReplyTo(c)}>
    <i className="fa-regular fa-comment" /> <span>回复</span>
  </button>
  <button className="comment-action-btn liked" onClick={() => toggleLike(c.id)}>
    <i className="fa-solid fa-heart" /> <span>0</span>
  </button>
</div>
```

**评论输入框标题**（ReaderClient.tsx 第 572-575 行）：
```html
<h3 className="comment-input-name" style={{ marginBottom: 16 }}>
  <i className="fa-regular fa-comments" style={{ marginRight: 8 }} />
  评论 ({totalComments})
</h3>
```

---

## 6. 经验教训

### 6.1 相同功能存在两套实现
- 楼中楼回复在 `ReaderClient.tsx` 和 `ParagraphCommentPanel.tsx` 中有两套独立的实现，样式和交互逻辑不一致，导致维护成本高
- **建议**：将楼中楼抽取为独立组件（如 `CommentThread.tsx`），统一管理和复用

### 6.2 浮动定位的计算方式需要优化
- 当前通过 `contentRef` 的 `getBoundingClientRect()` 计算 `panelRight`，在页面结构变化时可能不准确
- 过多的 `resize`/`scroll` 事件监听可能影响性能
- **建议**：考虑使用 CSS `position: sticky` 或 `container queries` 替代 JavaScript 计算

### 6.3 CSS 样式分散且有冗余
- 部分组件的样式分布在多个 CSS 文件中，且存在重复定义（如 `comment-action-btn` 在多处出现）
- 暗色模式的选择器嵌套较深，维护起来不够直观
- **建议**：统一使用 CSS 变量驱动颜色，减少重复选择器；考虑逐步迁移到 Tailwind utility class

### 6.4 数据加载策略可优化
- 评论数据在每次提交后重新加载全部（`loadComments()`），大数据量时效率低
- `comment_stats` 和 `comment_likes` 的查询可合并或使用视图
- **建议**：实现增量更新（乐观更新 + 追加新评论），减少全量重新加载

### 6.5 组件间通信方式
- 段评面板的 `onReport` 回调通过 props 传递，但 ReaderClient 和 ParagraphCommentPanel 之间共享的举报弹窗状态不够清晰
- 回复操作后需要重新加载评论列表，导致 UI 闪烁
- **建议**：考虑使用 Context 或回调 manager 来管理跨组件的交互状态

### 6.6 移动端适配
- 当前在 `max-width: 768px` 下隐藏了浮动侧边栏（`display: none`），但段评面板的触摸交互体验未优化
- 移动端评论区的间距和字体大小通过简单的媒体查询调整，缺乏精细的响应式设计
- **建议**：针对移动端重新设计段评交互方式（如左右滑动、长按触发），而非简单的隐藏

---

*文档生成日期：2026-08-03*
*基于 fanfic-platform 项目代码分析*