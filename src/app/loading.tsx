export default function Loading() {
  return (
    <main className="min-h-screen flex items-center justify-center bg-paper" aria-live="polite" aria-busy="true">
      <div className="text-sm text-muted">正在加载…</div>
    </main>
  );
}
